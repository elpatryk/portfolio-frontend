export const selectEvent = (reduxState) => reduxState.event.events;
export const selectByIdEvent = (reduxState) => reduxState.event.details;
export const selectMatches = (reduxState) => reduxState.event.matches;
