export const selectTeam = (reduxState) => reduxState.teams.teams;
export const userProfile = (reduxState) => reduxState.user.profile;
export const teamDetails = (reduxState) => reduxState.teams.details;
